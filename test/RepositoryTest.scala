import play.api.test._

/*
object RepositoryTest {
  def repositoryTestFakeApp = {
    val fakeApp = FakeApplication(
      // additionalConfiguration = inMemoryDatabase()
    )
    //Logger.info(s"FakeApplication created for test: $fakeApp")
    fakeApp
  }
}


abstract class RepositoryTest extends WithApplication(RepositoryTest.repositoryTestFakeApp) {
  // implicit lazy val session = models.MySlickDB.createSession
  val foo = "foo"
}
*/